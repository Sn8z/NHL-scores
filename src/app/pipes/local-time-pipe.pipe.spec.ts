import { LocalTimePipePipe } from './local-time-pipe.pipe';

describe('LocalTimePipePipe', () => {
  it('create an instance', () => {
    const pipe = new LocalTimePipePipe();
    expect(pipe).toBeTruthy();
  });
});
